apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: my-blueocaean-ingress
  namespace: gitlab
spec:
  rules:
  - host: gmt.blue.me
    http:
      paths:
      - path: /
        backend:
          serviceName: blueocean 
          servicePort: 80
